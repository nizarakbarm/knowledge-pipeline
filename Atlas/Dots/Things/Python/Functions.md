---
up:
  - "[[4 Major Python Language Features]]"
in:
  - "[[Things]]"
  - "[[Concepts]]"
created: 2025-09-05
---
- the definition begins with `def` keyword
- have zero or more formal **parameter**
- statement in the body begin one line after `def` and should be indented
- return value using `return` keyword
- no explicit `return` means return `None` (means nothing or null)
#### Calling Functions
- by name and followed by `()`, also uses dot (`.`) if calls functions of a class or module 